# Project music

Put music you are licensed to use here (for future clips whose source has no
usable music section). The pipeline NEVER downloads music from the internet.

Reference a file from a step record to underlay a section, e.g. add to the
section: "musicFile": "audio/my-track.mp3" (feature reserved — music already
present in the source video is always preferred).
